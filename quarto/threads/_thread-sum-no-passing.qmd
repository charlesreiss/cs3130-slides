
### sum example (only globals) {.smaller .longcode}

```{.c code-line-numbers=true}
{{< include /threads/sum-global-noid.c >}}
```

### sum example (only globals) {.smaller .longcode}

```{.c code-line-numbers=1}
{{< include /threads/sum-global-noid.c >}}
```

::: {.myoverbox .low}

values, results: global variables <br />
shared between all threads

:::

### sum example (only globals) {.smaller .longcode}

```{.c code-line-numbers="2-13"}
{{< include /threads/sum-global-noid.c >}}
```

::: {.myoverbox .vlow}

two functions --- same except some numbers <br />
<br />
values returned from via global array `results` <br />
used here instead of return value<br />
(partly to illustrate memory is shared; <br />
partly for later version that doesn't join)

:::
